[project]
name = "engauge-docs"
version = "0.1.0"
description = "Sphinx docs for Engauge Digitizer fork"
requires-python = ">=3.10"
dependencies = [
  "sphinx>=8.0",
  "furo>=2024.8.6"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
