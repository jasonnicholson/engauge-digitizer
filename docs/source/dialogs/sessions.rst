Sessions Dialog
===============

The Sessions dialog selects which settings are saved between sessions. The
first checkbox enables or disables the saving of settings between sessions.

When saving is enabled, the checkboxes beneath it determine which settings are
saved. These checkboxes correspond to the entries in the **Settings** menu.

.. image:: ../_images/dlgsessions.png
   :alt: Sessions dialog
   :align: center
