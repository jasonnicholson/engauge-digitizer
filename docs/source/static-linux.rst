Linux Static and Near-Static Strategy
=====================================

Near-static Linux portability is the current primary target.

Why:

- fully static glibc GUI builds are fragile across systems
- shared Qt distributions are the common baseline on Linux

Tracking and command log:

- see STATIC_BUILD_LINUX.md in repository root
